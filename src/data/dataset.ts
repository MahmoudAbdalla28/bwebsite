// Live counts from the cortex graph. Verified 2026-07-30 against
// localhost:6380 graph `bastion` (the cortex-local instance, not 6379).
// Re-read before changing. Never round up, never add a superlative.
export const dataset = {
  verified: '2026-07-30',
  engagements: 107,
  findings: 850,
  lessons: 694,
} as const

// Deliberately not published:
//   node count (20,551) is >half ingested public bug-bounty scope data
//   technique count (1,895) includes ~230 ingested from public sources
// Neither can carry the "generated by our own attacks" line.
